{
  "name": "@data-workers/dbt-connector",
  "version": "0.1.0",
  "description": "dbt Cloud API + manifest parsing connector",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": { "build": "tsc -b", "test": "vitest run", "clean": "rm -rf dist" },
  "license": "Apache-2.0"
}
