{
  "name": "@data-workers/validation",
  "type": "module",
  "version": "0.1.0",
  "description": "Hallucination reduction, sandboxing, and output validation",
  "main": "src/index.ts",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "license": "Apache-2.0"
}
